#!/bin/sh

for test_file in $(find ./ | grep "_test.cpp"); do
  source_file=$(echo "$test_file" | sed -e 's/_test//g')

  echo "[test] $test_file"
  ./env g++ -std=c++20 "$source_file" "$test_file" -L/usr/local/lib -lgtest -lgtest_main
  ./env ./a.out
done
